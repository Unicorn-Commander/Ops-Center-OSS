# BYOK (Bring Your Own Key) Configuration
# Copy to backend/.env.byok and fill in values

# === Encryption Key ===
# REQUIRED: Used to encrypt/decrypt API keys in Keycloak user attributes
# Generate new key with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-here

# === Keycloak Configuration ===
KEYCLOAK_URL=http://uchub-keycloak:8080
KEYCLOAK_REALM=uchub
KEYCLOAK_CLIENT_ID=ops-center
KEYCLOAK_CLIENT_SECRET=your-keycloak-client-secret

# Admin credentials for Keycloak API access
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=your-admin-password

# === System API Keys (Fallback) ===
# These keys are used when users don't have BYOK configured
# Optional - leave empty if not using fallback
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
HUGGINGFACE_TOKEN=
COHERE_API_KEY=
TOGETHER_API_KEY=
GROQ_API_KEY=
PERPLEXITY_API_KEY=

# === BYOK Settings ===
# Minimum tier required for BYOK (starter, professional, enterprise)
BYOK_MIN_TIER=starter
# Maximum keys per user
BYOK_MAX_KEYS_PER_USER=10
# Key validation on add (true/false)
BYOK_VALIDATE_ON_ADD=true
